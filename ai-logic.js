// УМНАЯ БАЗА ЗНАНИЙ ALLORA
export function getAIResponse(message) {
    const lowerMessage = message.toLowerCase().trim();
    
    // 1. КТО ТЫ
    if (lowerMessage.includes('ты кто') || lowerMessage.includes('кто ты')) {
        return `🤖 **Я AI Allora — ваш умный помощник!**

Я искусственный интеллект компании **Allora**, созданный для помощи клиентам и партнёрам.

**Что я умею:**
• Отвечать на любые вопросы
• Рассказывать об услугах Allora
• Консультировать по технологиям
• Собирать заявки для менеджеров

**База знаний:** обновлена до 2021 года.

Чем могу помочь?`;
    }

    // 2. ДАТА/ВРЕМЯ
    if (lowerMessage.includes('месяц') || lowerMessage.includes('день') || lowerMessage.includes('дата') || lowerMessage.includes('время') || lowerMessage.includes('число') || lowerMessage.includes('час')) {
        const now = new Date();
        const month = now.toLocaleString('ru-RU', { month: 'long' });
        const day = now.getDate();
        const year = now.getFullYear();
        const hours = now.getHours().toString().padStart(2, '0');
        const minutes = now.getMinutes().toString().padStart(2, '0');
        
        return `📅 **Сегодня:** ${day} ${month} ${year} года
🕒 **Время:** ${hours}:${minutes}

Всегда рад помочь с вопросами о Allora! Что ещё интересует?`;
    }

    // 3. ПРИВЕТСТВИЕ
    if (lowerMessage.includes('привет') || lowerMessage.includes('здравств') || lowerMessage.includes('добр') || lowerMessage === 'hi' || lowerMessage === 'hello') {
        return `👋 Привет! Я AI-ассистент компании **Allora**.

Чем могу помочь? Могу рассказать о:
• Наших услугах и специализациях
• Контактах для связи  
• Принципах работы
• Кейсах внедрения

Задайте ваш вопрос!`;
    }

    // 4. КАК ДЕЛА
    if (lowerMessage.includes('как дела') || lowerMessage.includes('как ты') || lowerMessage.includes('как жизнь')) {
        return `😊 Всё отлично! Готов помогать вам с вопросами об Allora и технологиях.

А у вас как дела? Чем могу помочь сегодня?`;
    }

    // 5. ALLORA / УСЛУГИ (ТРИГГЕР ДЛЯ ЛИДОВ!)
    if (lowerMessage.includes('allora') || lowerMessage.includes('аллора') || lowerMessage.includes('услуг') || lowerMessage.includes('чем занимает')) {
        return `🎯 **Услуги Allora:**

1. **Анализ данных и бизнес-аналитика**
2. **Машинное обучение и AI модели**
3. **Разработка ПО и веб-приложений**
4. **Автоматизация бизнес-процессов**
5. **Консалтинг по внедрению AI**

**Что именно интересует?**
(Спросите подробнее — после 2х вопросов предложу оставить контакты)`;
    }

    // 6. СТОИМОСТЬ (ТРИГГЕР ДЛЯ ЛИДОВ!)
    if (lowerMessage.includes('стоимость') || lowerMessage.includes('цен') || lowerMessage.includes('сколько стоит') || lowerMessage.includes('прайс')) {
        return `💰 **Стоимость услуг Allora:**

Рассчитывается индивидуально для каждого проекта.

**Факторы:**
• Сложность задачи
• Используемые технологии
• Сроки реализации
• Интеграции

✉️ *Для получения коммерческого предложения оставьте заявку.*`;
    }

    // 7. КОНТАКТЫ
    if (lowerMessage.includes('контакт') || lowerMessage.includes('связаться') || lowerMessage.includes('телефон') || lowerMessage.includes('почта')) {
        return `📞 **Контакты Allora:**

• **Email:** contact@allora.ai
• **Телефон:** +7 (XXX) XXX-XX-XX
• **Сайт:** https://allora.ai
• **Время работы:** Пн-Пт 9:00-18:00

Хотите оставить заявку?`;
    }

    // 8. ПРОЩАНИЕ
    if (lowerMessage.includes('пока') || lowerMessage.includes('до свидан') || lowerMessage.includes('спасиб') || lowerMessage.includes('благодар')) {
        return `👋 Всегда пожалуйста! Если понадобится помощь — обращайтесь!

Хорошего дня! 🚀`;
    }

    // 9. ДЕФОЛТНЫЙ ОТВЕТ (НЕ ПОВТОРЯЕТ ВОПРОС!)
    return `🤔 **По вашему вопросу:** "${message}"

Я AI-помощник компании **Allora**.

**Могу рассказать о:**
• Услугах Allora (аналитика, AI, разработка)
• Нашем технологическом стеке
• Контактах и процессе сотрудничества
• Кейсах и опыте

**Или ответить на общие вопросы.**

**Что конкретно вас интересует?** 
(Задайте вопрос об Allora — после 2х вопросов предложу оставить контакты)`;
}
