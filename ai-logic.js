// ALLORA AI с привязкой лидов к 2 вопросам
export function getAIResponse(message, isSecondAlloraQuestion = false) {
    const lowerMessage = message.toLowerCase().trim();
    
    // 1. ПРИВЕТСТВИЕ
    if (lowerMessage.includes('привет') || lowerMessage.includes('здравств') || lowerMessage.includes('добр')) {
        return `👋 Привет! Я AI-помощник **Allora**.

Я могу ответить на любые вопросы — про технологии, бизнес, финансы, ремонт или просто поговорить!

Чем могу помочь?`;
    }

    // 2. КТО ТЫ
    if (lowerMessage.includes('ты кто') || lowerMessage.includes('кто ты')) {
        return `🤖 **Я AI Allora** — ваш умный помощник от компании Allora, экспертов в AI и аналитике.

Отвечаю на любые вопросы и рассказываю о наших услугах.`;
    }

    // 3. ДАТА/ВРЕМЯ
    if (lowerMessage.includes('месяц') || lowerMessage.includes('день') || lowerMessage.includes('дата') || 
        lowerMessage.includes('время') || lowerMessage.includes('число') || lowerMessage.includes('час') ||
        lowerMessage.includes('сегодня') || lowerMessage.includes('какое сегодня')) {
        const now = new Date();
        const month = now.toLocaleString('ru-RU', { month: 'long' });
        const day = now.getDate();
        const year = now.getFullYear();
        const hours = now.getHours().toString().padStart(2, '0');
        const minutes = now.getMinutes().toString().padStart(2, '0');
        
        return `📅 **Сегодня:** ${day} ${month} ${year} года
🕒 **Время:** ${hours}:${minutes}`;
    }

    // 4. ALLORA / УСЛУГИ / СТОИМОСТЬ (ГЛАВНЫЕ ТРИГГЕРЫ ДЛЯ ЛИДОВ!)
    if (lowerMessage.includes('allora') || lowerMessage.includes('аллора') || 
        lowerMessage.includes('услуг') || lowerMessage.includes('чем занимает') ||
        lowerMessage.includes('стоимость') || lowerMessage.includes('цен') || 
        lowerMessage.includes('сколько стоит') || lowerMessage.includes('прайс') || 
        lowerMessage.includes('бюджет') || lowerMessage.includes('расценк')) {
        
        if (isSecondAlloraQuestion) {
            return `🎯 **По вашему вопросу о Allora:** "${message}"

${getAlloraDetailedResponse(lowerMessage)}

**ОЙ ДАВАЙТЕ С ВАМИ ПОЗНАКОМИМСЯ БЛИЖЕ ДЛЯ ПРОДОЛЖЕНИЯ НАШЕЙ БЕСЕДЫ!** 🎉

Чтобы продолжить обсуждение и получить детальную консультацию, оставьте, пожалуйста, ваши контакты. Наш специалист свяжется с вами!`;
        }
        
        return `🔍 **По вашему вопросу о Allora:** "${message}"

${getAlloraDetailedResponse(lowerMessage)}

Задавайте ещё вопросы!`;
    }

    // 5. БУХГАЛТЕРИЯ / ФИНАНСЫ
    if (lowerMessage.includes('бухгалтери') || lowerMessage.includes('налог') || 
        lowerMessage.includes('финанс') || lowerMessage.includes('отчётност')) {
        return `📊 **По вопросу о бухгалтерии/финансах:**

Allora помогает автоматизировать финансовые процессы:
• Интеграция бухгалтерских систем
• Автоматизация отчётности
• Анализ финансовых данных
• Дашборды для CFO`;
    }

    // 6. РЕМОНТ / СТРОИТЕЛЬСТВО
    if (lowerMessage.includes('ремонт') || lowerMessage.includes('строительств') || 
        lowerMessage.includes('квартир') || lowerMessage.includes('дом')) {
        return `🏠 **По вопросу о ремонте/строительстве:**

Мы разрабатываем:
• Системы "умный дом"
• CRM для строительных компаний
• Автоматизацию управления проектами`;
    }

    // 7. ТЕХНОЛОГИИ
    if (lowerMessage.includes('технолог') || lowerMessage.includes('it') || 
        lowerMessage.includes('компьют') || lowerMessage.includes('программ')) {
        return `💻 **Технологии:**

Мы работаем с: Python, JavaScript, TensorFlow, React, Docker, AWS и другими современными технологиями.`;
    }

    // 8. ОБЩИЕ ВОПРОСЫ
    if (lowerMessage.includes('как дела') || lowerMessage.includes('как ты') || 
        lowerMessage.includes('как жизнь') || lowerMessage.includes('что нового')) {
        return `😊 Всё отлично! Рад общению. Чем могу помочь?`;
    }

    if (lowerMessage.includes('спасиб') || lowerMessage.includes('благодар')) {
        return `🙏 Всегда пожалуйста!`;
    }

    if (lowerMessage.includes('пока') || lowerMessage.includes('до свидан')) {
        return `👋 До свидания!`;
    }

    // 9. ДЕФОЛТНЫЙ ОТВЕТ
    return `🤔 **Вы спрашиваете:** "${message}"

Я AI-помощник компании **Allora**. Могу ответить на различные вопросы или рассказать о наших услугах.

Что именно вас интересует?`;
}

// Детальные ответы про Allora
function getAlloraDetailedResponse(lowerMessage) {
    if (lowerMessage.includes('услуг') || lowerMessage.includes('чем занимает')) {
        return `**Услуги Allora:**
1. Анализ данных и бизнес-аналитика
2. Машинное обучение и AI-модели
3. Разработка ПО и веб-приложений
4. Автоматизация бизнес-процессов
5. Консалтинг по внедрению технологий`;
    }
    
    if (lowerMessage.includes('стоимость') || lowerMessage.includes('цен') || 
        lowerMessage.includes('сколько стоит') || lowerMessage.includes('бюджет')) {
        return `**Стоимость услуг Allora:**
Рассчитывается индивидуально после анализа:
• Задачи и объёма работ
• Используемых технологий
• Сроков реализации
• Необходимых интеграций`;
    }
    
    if (lowerMessage.includes('контакт') || lowerMessage.includes('связаться') || 
        lowerMessage.includes('телефон') || lowerMessage.includes('почта')) {
        return `**Контакты Allora:**
• Email: contact@allora.ai
• Телефон: +7 (XXX) XXX-XX-XX
• Сайт: https://allora.ai`;
    }
    
    return `**Компания Allora** — эксперты в области искусственного интеллекта и анализа данных.`;
}
